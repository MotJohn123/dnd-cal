# MongoDB
MONGODB_URI=mongodb://localhost:27017/dnd-calendar
# Or use MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/dnd-calendar

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-generate-with-openssl-rand-base64-32

# Google OAuth (for Calendar API)
# Option 1: OAuth with Refresh Token (requires user authentication)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/callback/google
GOOGLE_REFRESH_TOKEN=your-refresh-token

# Option 2: Service Account (recommended for server-side, easier setup)
# Create a service account in Google Cloud Console
# Download the JSON key file and extract these values:
# GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@project-id.iam.gserviceaccount.com
# GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Email (Nodemailer with Gmail)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM=DnD Calendar <your-email@gmail.com>

# Alternative: Use Resend for better email delivery
# RESEND_API_KEY=your-resend-api-key
